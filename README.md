# Pandas

Learned:

`import pandas as pd`: load **pandas** library with the pseudonym **pd**

`tmp = .DataFrame`: takes a list and sorts it

`tmp.head(number)`: show only the first few rows

`tmp.info()`: show type dates

`tmp.describe()`: immediate statistical summary
